apply plugin: 'application'

dependencies {
    compile project(':shared')
    compile 'com.github.insubstantial:substance:+'
}

mainClassName = 'com.booktion.client.BooktionClient'

jar {
    manifest.attributes 'Main-Class': 'com.booktion.server.BooktionClient'
    baseName = 'booktion-client'
}
