
subprojects {
    apply plugin: 'java'
    apply plugin: 'application'

    repositories {
        mavenCentral()
    }

    version = '0.0.1'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
